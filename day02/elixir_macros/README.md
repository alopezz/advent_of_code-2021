# Solution based on Elixir macros

A silly solution based on defining macros to make the input
executable, avoiding having to parse. Note that it's remarkably slow.

Run with:

```bash
elixir -r solver.ex input_exs
```
